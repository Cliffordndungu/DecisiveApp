@*Define model*@
@model IEnumerable<Downloads>

@*Define ViewData*@
@{
    ViewData["Title"] = "Download files";

}

<div class=" ="row">

    <div class="col-md-8 offset-md-2">

        <table class="table">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Name)</th>
                    <th>@Html.DisplayNameFor(model => model.Filepath)</th>
                    <th>@Html.DisplayNameFor(model => model.Description)</th>
                    <th>@Html.DisplayNameFor(model => model.OS)</th>
                  @*  @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                    {
                        <th>Actions></th>
                    }*@
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                       @* <td class="align-middle"> <img class="rounded-circle" src="@item.Name" alt="@item.Fullname" style="max-wdith: 150px" /> </td>*@
                        <td class="align-middle">@Html.DisplayFor(modelItem => item.Name) </td>
                        <td class="align-middle">@Html.DisplayFor(modelItem => item.Description) </td>
                        <td class="align-middle">@Html.DisplayFor(modelItem => item.Filepath) </td>
                        <td class="align-middle">@Html.DisplayFor(modelItem => item.OS) </td>
                       @* @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                        {
                            <td class="align-middle">
                                <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"> <i class="bi bi-penciil-square"> </i> Edit  </a>
                                <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@item.Id"> <i class="bi bi-eye"></i>Details</a>
                                <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="@item.Id">
                                    <i class="bi bi-trash">
                                    </i>
                                    Delete
                                </a>
                            </td>
                        }*@
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
@*<div style="position: fixed; right:25px; bottom:25px;" class="text-white">
    <a asp-controller="Actors" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle"></i> Add New</a>
</div>*@

